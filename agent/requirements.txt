# LiveKit Voice Agent Dependencies
# Python 3.10+ required

# Core LiveKit Agents framework with Silero VAD and turn detection
livekit-agents[silero,turn-detector]~=1.0

# Groq LLM plugin for fast inference
livekit-plugins-groq~=1.0

# Deepgram plugin for STT and TTS
livekit-plugins-deepgram~=1.0

# Sarvam AI plugin for Hindi TTS (official LiveKit plugin)
# Supports Indian language voice AI with Bulbul TTS model
livekit-plugins-sarvam

# Noise cancellation for AEC (Acoustic Echo Cancellation)
livekit-plugins-noise-cancellation~=0.2

# Environment variable loading
python-dotenv~=1.0

# Tavily AI for real-time web search
tavily-python~=0.5

# Notion API client for conversation logging
notion-client~=2.2

# Resend for cloud-compatible email delivery (SMTP blocked on Railway)
resend~=2.0
